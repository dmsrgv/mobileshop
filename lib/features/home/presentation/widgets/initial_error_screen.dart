import 'package:flutter/material.dart';

class InitialErrorScreen extends StatelessWidget {
  const InitialErrorScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('INITIAL ERROR'),
    );
  }
}
